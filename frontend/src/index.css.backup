/* MINDFULME MOBILE RESPONSIVE CSS */

/* Reset and Base Styles */
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
  -webkit-tap-highlight-color: transparent;
}

html {
  font-size: 16px;
  -webkit-text-size-adjust: 100%;
  -ms-text-size-adjust: 100%;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
  line-height: 1.6;
  color: #333;
  overflow-x: hidden;
  background: #f8fafc;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

#root {
  width: 100%;
  min-height: 100vh;
  overflow-x: hidden;
}

/* App Container */
.App {
  width: 100%;
  min-height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

/* Mobile-First Responsive Breakpoints */
/* Mobile: 0-767px (default) */
/* Tablet: 768px-1023px */
/* Desktop: 1024px+ */

/* =========================
   MOBILE STYLES (DEFAULT)
   ========================= */

/* Navigation Fixes */
.navigation {
  padding: 0.75rem 1rem !important;
  height: auto !important;
  min-height: 60px;
}

.nav-logo {
  font-size: 1.2rem !important;
}

.mobile-menu-toggle {
  width: 35px !important;
  height: 35px !important;
  font-size: 1rem !important;
}

/* Dashboard Container */
.dashboard-container,
div[style*="maxWidth: '1400px'"] {
  margin: 0.5rem !important;
  max-width: calc(100vw - 1rem) !important;
  padding: 0 !important;
}

/* Welcome Card - Mobile Optimized */
div[style*="Welcome back"] {
  padding: 1.5rem 1rem !important;
  margin-bottom: 1.5rem !important;
  border-radius: 15px !important;
}

/* Welcome Title */
h1[style*="Welcome back"] {
  font-size: 1.8rem !important;
  line-height: 1.2 !important;
  margin: 0.5rem 0 !important;
}

/* Welcome Description */
p[style*="How are you feeling"] {
  font-size: 1rem !important;
  line-height: 1.4 !important;
}

/* Stats Grid - Mobile Stack */
div[style*="gridTemplateColumns"] {
  grid-template-columns: 1fr 1fr !important;
  gap: 1rem !important;
  margin-bottom: 1.5rem !important;
}

/* Individual Stat Cards */
div[style*="background: linear-gradient"][style*="padding"] {
  padding: 1.25rem 0.75rem !important;
  border-radius: 15px !important;
  min-height: 120px !important;
}

/* Stat Card Numbers */
div[style*="fontSize"][style*="2.5rem"] {
  font-size: 1.8rem !important;
}

/* Stat Card Icons */
div[style*="fontSize"][style*="3.5rem"] {
  font-size: 2.5rem !important;
  margin-bottom: 0.75rem !important;
}

/* Daily Inspiration */
div[style*="Daily Inspiration"] {
  padding: 1.5rem 1rem !important;
  margin-bottom: 1.5rem !important;
  border-radius: 15px !important;
}

/* Inspiration Text */
p[style*="fontStyle: 'italic'"] {
  font-size: 1rem !important;
  line-height: 1.5 !important;
}

/* Quick Actions Grid */
div[style*="repeat(2, 1fr)"] {
  grid-template-columns: 1fr !important;
  gap: 1rem !important;
}

/* Quick Action Cards */
div[style*="Quick Journal"],
div[style*="Breathing Exercise"] {
  padding: 1.5rem 1rem !important;
  border-radius: 15px !important;
}

/* Quick Action Icons */
div[style*="fontSize: '5rem'"] {
  font-size: 3.5rem !important;
  margin-bottom: 1rem !important;
}

/* Quick Action Titles */
h3[style*="1.8rem"] {
  font-size: 1.4rem !important;
  margin-bottom: 0.75rem !important;
}

/* Quick Action Descriptions */
p[style*="Write down your thoughts"],
p[style*="Practice mindful breathing"] {
  font-size: 0.95rem !important;
  margin-bottom: 1.5rem !important;
}

/* Buttons */
button[style*="Start Writing"],
button[style*="Start Session"] {
  padding: 0.75rem 1.5rem !important;
  font-size: 1rem !important;
  border-radius: 12px !important;
  width: 100% !important;
}

/* Emergency Support */
div[style*="Need Immediate Help"] {
  padding: 1.5rem 1rem !important;
  border-radius: 15px !important;
  margin-bottom: 1rem !important;
}

/* Emergency Title */
h3[style*="Need Immediate Help"] {
  font-size: 1.5rem !important;
  margin-bottom: 0.75rem !important;
}

/* Emergency Description */
p[style*="crisis support"] {
  font-size: 1rem !important;
  margin-bottom: 1.5rem !important;
}

/* Loading Screen */
div[style*="Loading your wellness"] {
  padding: 1rem !important;
}

div[style*="Loading your wellness"] div[style*="fontSize: '4rem'"] {
  font-size: 3rem !important;
  margin-bottom: 1rem !important;
}

div[style*="Loading your wellness"] div[style*="fontSize: '1.8rem'"] {
  font-size: 1.4rem !important;
}

/* =================
   TABLET STYLES
   ================= */
@media (min-width: 768px) {
  /* Stats Grid - 2 columns on tablet */
  div[style*="gridTemplateColumns"] {
    grid-template-columns: repeat(2, 1fr) !important;
    gap: 1.5rem !important;
  }
  
  /* Increase padding on tablet */
  div[style*="Welcome back"] {
    padding: 2rem 1.5rem !important;
  }
  
  /* Welcome Title */
  h1[style*="Welcome back"] {
    font-size: 2.2rem !important;
  }
  
  /* Stat Cards */
  div[style*="background: linear-gradient"][style*="padding"] {
    padding: 1.75rem 1.25rem !important;
    min-height: 140px !important;
  }
  
  /* Quick Actions back to 2 columns */
  div[style*="repeat(2, 1fr)"] {
    grid-template-columns: repeat(2, 1fr) !important;
    gap: 1.5rem !important;
  }
  
  /* Navigation */
  .navigation {
    padding: 1rem 1.5rem !important;
  }
  
  .nav-logo {
    font-size: 1.5rem !important;
  }
}

/* =================
   DESKTOP STYLES
   ================= */
@media (min-width: 1024px) {
  /* Stats Grid - 4 columns on desktop */
  div[style*="gridTemplateColumns"] {
    grid-template-columns: repeat(4, 1fr) !important;
    gap: 2rem !important;
  }
  
  /* Container max width */
  .dashboard-container,
  div[style*="maxWidth: '1400px'"] {
    max-width: 1200px !important;
    margin: 2rem auto !important;
    padding: 0 2rem !important;
  }
  
  /* Restore desktop sizes */
  h1[style*="Welcome back"] {
    font-size: 2.8rem !important;
  }
  
  div[style*="background: linear-gradient"][style*="padding"] {
    padding: 2.25rem 1.75rem !important;
    min-height: 160px !important;
  }
  
  div[style*="fontSize"][style*="2.5rem"] {
    font-size: 2.2rem !important;
  }
  
  div[style*="fontSize"][style*="3.5rem"] {
    font-size: 3.2rem !important;
  }
  
  /* Navigation */
  .navigation {
    padding: 1rem 2.5rem !important;
  }
  
  .nav-logo {
    font-size: 1.8rem !important;
  }
}

/* =================
   LARGE DESKTOP
   ================= */
@media (min-width: 1440px) {
  .dashboard-container,
  div[style*="maxWidth: '1400px'"] {
    max-width: 1400px !important;
    padding: 0 3rem !important;
  }
}

/* =================
   TOUCH OPTIMIZATIONS
   ================= */
@media (hover: none) and (pointer: coarse) {
  /* Increase touch targets */
  button, 
  div[style*="cursor: 'pointer'"] {
    min-height: 44px !important;
    min-width: 44px !important;
  }
  
  /* Remove hover effects on touch devices */
  button:hover,
  div[style*="cursor: 'pointer'"]:hover {
    transform: none !important;
  }
  
  /* Add active states for touch feedback */
  button:active,
  div[style*="cursor: 'pointer'"]:active {
    transform: scale(0.97) !important;
    opacity: 0.8 !important;
  }
}

/* =================
   SAFE AREAS (iPhone notch, etc.)
   ================= */
@supports (padding: max(0px)) {
  .navigation {
    padding-left: max(1rem, env(safe-area-inset-left)) !important;
    padding-right: max(1rem, env(safe-area-inset-right)) !important;
    padding-top: max(0.75rem, env(safe-area-inset-top)) !important;
  }
  
  .dashboard-container,
  div[style*="maxWidth: '1400px'"] {
    padding-left: max(0.5rem, env(safe-area-inset-left)) !important;
    padding-right: max(0.5rem, env(safe-area-inset-right)) !important;
  }
}

/* =================
   ANIMATIONS & PERFORMANCE
   ================= */
* {
  scroll-behavior: smooth;
}

/* Reduce motion for users who prefer it */
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

/* Optimize font rendering */
@media screen and (-webkit-min-device-pixel-ratio: 2) {
  body {
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }
}

/* =================
   MODAL RESPONSIVE FIXES
   ================= */
div[style*="position: fixed"][style*="zIndex: 1000"],
div[style*="position: fixed"][style*="zIndex: 1001"] {
  padding: 1rem !important;
}

div[style*="maxWidth: '800px'"],
div[style*="maxWidth: '500px'"] {
  max-width: 95vw !important;
  width: 100% !important;
  max-height: 85vh !important;
  overflow-y: auto !important;
}

/* Modal content padding */
div[style*="padding: '2.5rem'"] {
  padding: 1.5rem !important;
}

@media (min-width: 768px) {
  div[style*="maxWidth: '800px'"],
  div[style*="maxWidth: '500px'"] {
    max-width: 90vw !important;
  }
  
  div[style*="padding: '2.5rem'"] {
    padding: 2rem !important;
  }
}

@media (min-width: 1024px) {
  div[style*="maxWidth: '800px'"] {
    max-width: 800px !important;
  }
  
  div[style*="maxWidth: '500px'"] {
    max-width: 500px !important;
  }
  
  div[style*="padding: '2.5rem'"] {
    padding: 2.5rem !important;
  }
}

/* =================
   UTILITIES
   ================= */
.mobile-only {
  display: block;
}

.tablet-up {
  display: none;
}

.desktop-only {
  display: none;
}

@media (min-width: 768px) {
  .mobile-only {
    display: none;
  }
  
  .tablet-up {
    display: block;
  }
}

@media (min-width: 1024px) {
  .desktop-only {
    display: block;
  }
}

/* Force hardware acceleration for smooth animations */
.dashboard-container,
div[style*="background: linear-gradient"] {
  transform: translateZ(0);
  will-change: transform;
}

/* Admin Dashboard Styles */
.admin-dashboard {
  padding: 20px;
  min-height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.admin-header {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  border-radius: 20px;
  padding: 30px;
  margin-bottom: 30px;
  border: 1px solid rgba(255, 255, 255, 0.2);
}

.header-content {
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
}

.admin-title h1 {
  color: white;
  margin: 0 0 10px 0;
  font-size: 2.5rem;
}

.admin-title p {
  color: rgba(255, 255, 255, 0.8);
  margin: 0;
}

.refresh-button {
  background: rgba(255, 255, 255, 0.2);
  border: 1px solid rgba(255, 255, 255, 0.3);
  color: white;
  padding: 12px 20px;
  border-radius: 10px;
  cursor: pointer;
  transition: all 0.3s ease;
}

.refresh-button:hover {
  background: rgba(255, 255, 255, 0.3);
}

.status-cards {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 20px;
  margin-bottom: 30px;
}

.status-card {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  border-radius: 15px;
  padding: 20px;
  border: 1px solid rgba(255, 255, 255, 0.2);
  display: flex;
  align-items: center;
  gap: 15px;
}

.status-icon {
  font-size: 2rem;
}

.status-content h3 {
  color: white;
  margin: 0 0 5px 0;
}

.status-content p {
  color: rgba(255, 255, 255, 0.8);
  margin: 0;
  font-size: 0.9rem;
}

.admin-tabs {
  display: flex;
  gap: 10px;
  margin-bottom: 30px;
  flex-wrap: wrap;
}

.tab {
  background: rgba(255, 255, 255, 0.1);
  border: 1px solid rgba(255, 255, 255, 0.2);
  color: white;
  padding: 12px 20px;
  border-radius: 10px;
  cursor: pointer;
  transition: all 0.3s ease;
}

.tab.active {
  background: rgba(255, 255, 255, 0.3);
  border-color: rgba(255, 255, 255, 0.5);
}

.tab:hover {
  background: rgba(255, 255, 255, 0.2);
}

.stats-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 20px;
}

.stat-card {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  border-radius: 15px;
  padding: 25px;
  border: 1px solid rgba(255, 255, 255, 0.2);
  text-align: center;
}

.stat-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 15px;
}

.stat-header h3 {
  color: white;
  margin: 0;
  font-size: 1rem;
}

.stat-icon {
  font-size: 1.5rem;
}

.stat-value {
  font-size: 3rem;
  font-weight: bold;
  color: white;
  margin-bottom: 10px;
}

.stat-change {
  color: rgba(255, 255, 255, 0.8);
  font-size: 0.9rem;
}

.users-list {
  display: grid;
  gap: 15px;
}

.user-card {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  border-radius: 15px;
  padding: 20px;
  border: 1px solid rgba(255, 255, 255, 0.2);
  display: flex;
  align-items: center;
  gap: 15px;
  flex-wrap: wrap;
}

.user-avatar {
  width: 50px;
  height: 50px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.3);
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-weight: bold;
  font-size: 1.2rem;
}

.user-info h4 {
  color: white;
  margin: 0 0 5px 0;
}

.user-info p {
  color: rgba(255, 255, 255, 0.8);
  margin: 0;
  font-size: 0.9rem;
}

.user-stats {
  display: flex;
  gap: 15px;
  margin-left: auto;
}

.user-stat {
  color: rgba(255, 255, 255, 0.9);
  font-size: 0.9rem;
}

.activity-status {
  padding: 5px 10px;
  border-radius: 15px;
  font-size: 0.8rem;
  font-weight: bold;
}

.activity-status.high { background: #4ade80; color: #065f46; }
.activity-status.medium { background: #fbbf24; color: #92400e; }
.activity-status.low { background: #f87171; color: #991b1b; }
.activity-status.inactive { background: #6b7280; color: white; }

.error-banner {
  background: rgba(239, 68, 68, 0.2);
  border: 1px solid rgba(239, 68, 68, 0.3);
  color: white;
  padding: 15px;
  border-radius: 10px;
  margin-bottom: 20px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.error-banner button {
  background: rgba(255, 255, 255, 0.2);
  border: 1px solid rgba(255, 255, 255, 0.3);
  color: white;
  padding: 8px 15px;
  border-radius: 5px;
  cursor: pointer;
}

.loading-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 400px;
  color: white;
}

.loading-spinner {
  width: 50px;
  height: 50px;
  border: 4px solid rgba(255, 255, 255, 0.3);
  border-top: 4px solid white;
  border-radius: 50%;
  animation: spin 1s linear infinite;
  margin-bottom: 20px;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

.analytics-cards {
  display: grid;
  gap: 20px;
}

.analytics-card {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  border-radius: 15px;
  padding: 25px;
  border: 1px solid rgba(255, 255, 255, 0.2);
}

.analytics-card h3 {
  color: white;
  margin: 0 0 20px 0;
}

.engagement-item {
  display: flex;
  justify-content: space-between;
  margin-bottom: 10px;
  color: rgba(255, 255, 255, 0.9);
}

.engagement-item strong {
  color: white;
}

.health-item {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 10px;
  color: rgba(255, 255, 255, 0.9);
}

.health-dot {
  width: 12px;
  height: 12px;
  border-radius: 50%;
}

.health-dot.success {
  background: #4ade80;
}

.access-denied {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 400px;
  color: white;
  text-align: center;
}

.access-denied h2 {
  margin-bottom: 10px;
}

/* Responsive Design */
@media (max-width: 768px) {
  .admin-dashboard {
    padding: 10px;
  }
  
  .header-content {
    flex-direction: column;
    gap: 20px;
  }
  
  .status-cards {
    grid-template-columns: 1fr;
  }
  
  .stats-grid {
    grid-template-columns: 1fr;
  }
  
  .user-card {
    flex-direction: column;
    align-items: flex-start;
  }
  
  .user-stats {
    margin-left: 0;
  }
}
